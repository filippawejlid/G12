class Vacuum {

    productName: string;
    price: number;
    model: string;
    id: number;

    constructor (name: string, price: number, model: string, Id: number) { 

        this.productName = name;
        this.price = price;
        this.model = model;
        this.id = Id;
    }
}